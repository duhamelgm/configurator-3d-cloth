{
  "name": "3d-configurator",
  "version": "1.0.0",
  "description": "3D configurator demo",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Duhamel Guerrero",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
